<%= form_for @client, :url => url_for(:controller => 'clients', :action => 'update') do |f| %>
  <%= f.label :weight_change, "How many lbs. were lost/gained since last appointment?" %>
  <br></br>
  <%= f.number_field :weight_change %>
  <br></br>
  <%= f.fields_for :appointments, @client.appointments.last do |app| %>
    <%= app.label :date, "Schedule Next Appointment:" %>
    <br></br>
    <%= app.datetime_select :date, order: [:month, :day, :year, :hour], ampm: true %>
  <% end %>
  <br></br>
  <%= f.submit %>
<% end %>