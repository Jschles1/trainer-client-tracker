<%= form_for @appointment do |f| %>
  <%= f.label :date, "Appointment Date & Time" %>
  <br></br>
  <%= select_datetime(@appointment[:date], order: [:month, :day, :year, :hour], ampm: true) %>
  <%= f.submit %>
<% end %>
