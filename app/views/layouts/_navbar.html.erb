<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
    <% if !logged_in? %>
      <ul class="nav navbar-nav">
        <li><%= link_to "Log In", login_path %></li>
        <li><%= link_to "Sign Up", signup_path %></li>
        <li><%= link_to "Sign in with Facebook", "/auth/facebook", id: "sign_in" %></li>
      </ul>
    <% else %>
      <p class="navbar-brand">Signed in as: <%= current_user.name %></p>
      <ul class="nav navbar-nav">
        <li><a href="#" onClick="loadClients()">My Clients</a></li>
        <li><%= link_to "My Appointments", user_appointments_path(current_user) %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Log Out", logout_path %></li>
      </ul>
    <% end %>
    </div>
  </div>
</nav>
